=== Overview

This group defines a "bugreport" service that call dumpstate command to
collect system status and can be triggered by keycodes.

=== Options

--- default
when not explicitly selected in mixin spec file, the default option will be used.
And the "bugreport" service can be triggered by holding down volume down, volume up and power.

