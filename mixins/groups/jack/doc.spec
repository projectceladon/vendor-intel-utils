=== Overview

The new jack java compiler introduced by Google on M, is crashing many
buildbot machine. the crash is unrecoverable, as the jack process cannot be killed with
kill -9 disabling jack as a temporary workaround, until google release a fix.


=== Options

--- default
when not explicitly selected in mixin spec file, the default option will be used.
It links false option will disabe jack default.



