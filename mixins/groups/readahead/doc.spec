=== Overview

Readahead speeds up file access (Read operation on disk) by pre-fetching data and loading it into RAM.

=== Options

--- optimized
enable readahead optimization feature.

    --- parameters
        - size: readahead size


--- default
when not explicitly selected in mixin spec file, the default option will be used.
It's empty dir.


