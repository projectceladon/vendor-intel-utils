[output.installer.cmd]
additional-files += radio:factory.img

[group.erase-factory]
name = Erase factory partition
description = Erase factory partition, use with caution
default = false

[command.format.factory]
tool = fastboot
args = format factory
description = Format factory partition
group = erase-factory
