From c996ea8218f19dae46edb76b526a56987ab4c358 Mon Sep 17 00:00:00 2001
From: yingzhex <yingzhenx.li@intel.com>
Date: Thu, 5 Nov 2020 10:56:02 +0800
Subject: [PATCH] Remove oneway log before root cause

Change-Id: I0ecdcc8924767926c42c8978de871e120e9e5392
Tracked-On: ACP-1278
Signed-off-by: yingzhex <yingzhenx.li@intel.com>
---
 libs/binder/IPCThreadState.cpp | 5 -----
 1 file changed, 5 deletions(-)

diff --git a/libs/binder/IPCThreadState.cpp b/libs/binder/IPCThreadState.cpp
index d67ce15ca..6f4481949 100644
--- a/libs/binder/IPCThreadState.cpp
+++ b/libs/binder/IPCThreadState.cpp
@@ -1234,11 +1234,6 @@ status_t IPCThreadState::executeCommand(int32_t cmd)
                 if (error < NO_ERROR) reply.setError(error);
                 sendReply(reply, 0);
             } else {
-                if (error != OK || reply.dataSize() != 0) {
-                    alog << "oneway function results will be dropped but finished with status "
-                         << statusToString(error)
-                         << " and parcel size " << reply.dataSize() << endl;
-                }
                 LOG_ONEWAY("NOT sending reply to %d!", mCallingPid);
             }
 
-- 
2.25.1

