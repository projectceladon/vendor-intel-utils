From 9bfbca0b52d00f680f6f8e0d17bdf2b2b1c7079e Mon Sep 17 00:00:00 2001
From: "Chen, Yu" <yu.y.chen@intel.com>
Date: Mon, 12 Oct 2020 13:27:06 +0800
Subject: [PATCH] add more log

---
 VolumeManager.cpp | 9 +++++++--
 1 file changed, 7 insertions(+), 2 deletions(-)

diff --git a/VolumeManager.cpp b/VolumeManager.cpp
index 95fef48..951fbb9 100644
--- a/VolumeManager.cpp
+++ b/VolumeManager.cpp
@@ -237,13 +237,18 @@ void VolumeManager::handleBlockEvent(NetlinkEvent* evt) {
                     int flags = source->getFlags();
                     if (major == kMajorBlockMmc || IsVirtioBlkDevice(major)) {
                         flags |= android::vold::Disk::Flags::kSd;
+            		LOG(DEBUG) << "Recognized as sdcard!!!!!!!!!!!!!!!!";
                     } else {
 			// FIXME: In CIV KVM SD card is virtualized through hdd now,
 			// kMajorBlockMmcCIV and below block need to be  removed.
-			if (major == kMajorBlockMmcCIV && source->getNickname() == "sdcard1")
+			if (major == kMajorBlockMmcCIV && source->getNickname() == "sdcard1") {
 				flags |= android::vold::Disk::Flags::kSd;
-			else
+            			LOG(DEBUG) << "Recognized as sdcard!!!!!!!!!!!!!!!!";
+			}
+			else {
 				flags |= android::vold::Disk::Flags::kUsb;
+            			LOG(DEBUG) << "Recognized as usb!!!!!!!!!!!!!!!!!";
+			}
                     }
 
                     auto disk =
-- 
2.28.0

